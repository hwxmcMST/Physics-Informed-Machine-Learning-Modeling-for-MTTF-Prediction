function NewSite=RoundSite(OldSite, StandardSite)
% the Z points generated by Hammersley may not be included in the Z mesh,
% in this case, we should round the non-standard sites into the standard
% ones.

NewSite=OldSite;

n=length(OldSite);
for i=1:n
    A=StandardSite-OldSite(i);
    A=A.^2;
    [~,index]=min(A);
    NewSite(i)=StandardSite(index);
end